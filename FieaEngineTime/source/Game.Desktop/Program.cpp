#include "pch.h"
#include "SList.h"
#include "Vector.h"
#include "HashMap.h"
#include "TypeRegistry.h"
//#include "AttributedFoo.h"
#include "Attributed.h"
#include <cstdint>
#include <iostream>

using namespace FieaGameEngine;

int main()
{
	//Signature signature{ "ExternalInteger", Datum::DatumTypes::Integer, 1, 1 };
	//Vector<Signature> mVector;
	//mVector.PushBack(signature);
	//TypeRegistry& registry = registry.GetInstance();
	//std::pair<size_t, Vector<Signature>> pair = std::make_pair(1, mVector);
	//registry.SetSignatures(pair);

	//TypeRegistry& registry = registry.GetInstance();									
	//registry.SetSignatures(std::make_pair(AttributedFoo::TypeIdClass(), AttributedFoo::Signatures()));

	//RegisterType(AttributedFoo, Attributed)

	return 0;
}